#include <stdio.h>

// 2D Array = An array, where each element is an entire array
//            useful if you need a matrix, grid or table of data

int main(){

    int numbers[2][3] = {
                        {1, 2, 3},
                        {4, 5, 6}
                        };

    //you have to specify a maximum size of elements within each of these arrays, such as "[2][3]"
    //[2] 2 because we have 2 separate arrays within our 2D array (number of rows)
    //[3] is the number of columns

    return 0;
}